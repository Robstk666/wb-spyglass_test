meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"
build:
  requirementsPath: requirements.txt
run:
  command: uvicorn server:app --host 0.0.0.0 --port 8000
  containerPort: 8000
  servicePort: 8000
